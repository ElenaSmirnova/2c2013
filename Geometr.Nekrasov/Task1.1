#include <stdio.h>
#include <stdlib.h>



int main()
{
  int b, n, i, current;
  int a[20];
  char s[100];

i=0;
  do
  {
     gets(s);
     //puts(s);
     if ((s[0]=='+')or(s[0]=='-')or(s[0]=='*')or(s[0]=='/'))
       { //printf("%c\n",'$');
            if (s[0]=='+')
            {
              // b=a[i-1];
               a[i-2]=a[i-2]+a[i-1];
               a[i-1]=0;
               i=i-1;
               //printf("%c\n",'$');
               printf("%d\n", a[i-1]);
            }
            else
            {
                  if (s[0]=='-')
                  {
                    a[i-2]=a[i-2]-a[i-1];
                  }
                   else
                  {
                       if (s[0]=='*')
                       {
                           a[i-2]=a[i-2]*a[i-1];
                       }
                       else
                       {
                           if(a[i-1]!=0)
                           {
                               a[i-2]=a[i-2]/a[i-1];
                           }
                           else
                           {
                               printf("\n");
                               break;
                           }
                       }
                  }
            }
       }
      else
       {
         if (s[0]!='e')
         {
             int b= atoi(s);
             a[i]=b;
             printf("\n%d\n", a[i]);
             b=0;
             i=i+1;
         }
         else
         {
             break;
         }
       }
  }
  while (1);
  printf("%d\n", a[0]);
}